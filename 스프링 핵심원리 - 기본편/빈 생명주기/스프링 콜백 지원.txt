1. 인터페이스 InitializingBean, DisposableBean - 초창기 설정 방법
 - InitializingBean : afterPropertiesSet() 메서드를 지원하여 의존관계 주입이 종료되면 호출
 - DisposableBean : destroy() 메서드를 지원하여 종료시점에 호출 

# 초기화, 소멸 인터페이스의 단점
 - 스프링 전용 인터페이스에 의존 
 - 초기화, 소멸 메서드의 이름을 변경할 수 없다.
 - 외부 라이브러리에 적용할 수 없다.

2. 빈 등록 시점에 지정하기
 - @Bean(initMethod = "init", destroyMethod = "close") 초기화, 소멸 메서드 지정

# 빈 등록 장점
 - 스프링에 의존하지 않음
 - 메서드 이름을 자유롭게 줄 수 있다.
 - 코드가 아닌 설정 정보를 사용하기 때문에 코드를 고칠 수 없는 외부 라이브러리에도 초기화, 종료 메서드를 적용할 수 있다.

# 종료 메서드 추론
 - close, shutdown 라는 이름의 메서드를 자동으로 호출해준다.
 - 따라서 직접 스프링 빈으로 등록하면 종료 메서드는 따로 적어주지 않아도 잘 동작한다.
 - 추론 기능을 사용하지 않는다면 destroyMethod = "" 공백으로 지정한다.

3. 어노테이션 사용 @PostConstruct, @PreDestroy - 권장방식
 - 어노테이션만 붙이면 사용가능 

유일한 단점! 
외부 라이브러리에는 적용하지 못한다는 것! (스프링 컨테이너에서 관리하는 빈에서만 사용할 수 있다.)
