Enum : 상수를 표현

만약 정수로만 표현한다면, 정해진 값만 변수에 할당할 수 없다는 문제점이 발생! 
-> Enum 클래스 표현하여 해결

장점 
 - 타입에 대해 안전하다. 

EnumMap : Enum 타입의 키값을 가지고 value를 표현할 때 사용 

#Enum의 메모리 생성시기
Enum객체는 Enum 상수가 처음 호출되거나 참조될 때 생성된다.